import { Section } from '@/Components/Section';

export const Error = () => {
    return (
        <Section title={'Ошибка'}>
            <h3 style={{ textAlign: 'center' }}>При загрузке произошла ошибка</h3>
        </Section>
    );
};
